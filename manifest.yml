---
defaults: &defaults  
  path: .
  domain: mybluemix.net
  disk_quota: 1024M
  instances: 1
  command: node index.js

applications:
  - name: STAGING OFFSET 
    host: staging-offset
    memory: 64MB
    <<: defaults

  - name: OFFSET 
    host: offset
    memory: 128MB
    <<: defaults


