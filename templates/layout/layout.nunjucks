<!DOCTYPE html>
<html class="html no-js" lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, minimal-ui">
    <title>{{ name }}</title>
    <link rel="stylesheet" href="/css/main.css">

    {% block head -%}
    {%- endblock -%}
  </head>
  <body class="body">

    <main class="main sy--dark-ui" {%- if route %} data-{{ route }} {%- endif -%} {%- if content %} data-questions-length="{{ content.attributes.questions | length }}" {%- endif -%}>

      {%- block content -%}
      {%- endblock -%}
    </main>

    {% block script -%}
    {%- endblock -%}
  </body>
